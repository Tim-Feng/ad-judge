<div id="main_items" class="center main_items_decoration">
  <% @posts.each do |post| %>
    <%= render 'post', post: post %>
  <% end %>

<div class="paginate">
  <%= paginate @posts, remote: true %>
</div>