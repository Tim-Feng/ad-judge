<% if post.up_ratio >= post.down_ratio %>
  <p class="vote_result_text">
    <%= raw number_to_percentage(post.up_ratio, precision: 0) %>的人喜歡這支廣告
  </p>
<% elsif post.up_ratio < post.down_ratio %>
  <p class="vote_result_text">
    <%= raw number_to_percentage(post.down_ratio, precision: 0) %>的人討厭這支廣告
  </p>
<% else %>
  <p class="vote_result_text">
    還沒有人投票
  </p>
<% end %>
<div class="progress span3 center">
  <div class="bar bar-success" style= "width: <%=raw number_to_percentage(post.up_ratio, precision: 2) %>%;"></div>
  <div class="bar bar-danger" style= "width: <%=raw number_to_percentage(post.down_ratio, precision: 2) %>%;" ></div>
</div>